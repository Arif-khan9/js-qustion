// Write a program that takes an integer input and prints whether it's even or odd.
function isEven(num){
    if(num % 2 === 0){
        return`${num} is even`;
    }else{
        return`${num} is odd`;
    }
}
